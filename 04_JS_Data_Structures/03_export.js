export const name = "Mike";
export const sample = "Sample text";

const info = () => {
    let age = 25;
    let address = "Manila";
    return 'I am ' + age + ' years old. And Im from ' + address;
};

export default info;
